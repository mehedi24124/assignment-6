1) What is the difference between null and undefined?
=> Null is basically define by a variable with no value assigned. it means variable is empty.And undefined is stands for a variable is declered but not assigned a value. it means something is missing in variable.

2) What is the use of the map() function in JavaScript? How is it different from forEach()?
=>map is used for creating a new array applying on function. Also used for tranforming data. It's an array method which is used to loop through ecah elements of an arry.

3) What is the difference between == and ===?
 == is used for comparring tow values after covertiong their type. it checks only value. And === is check value + type at the same time.

4) What is the significance of async/await in fetching API data?
=> async/await is used for handle API calls in clean and readable way.it makes code more easyer to debug or explain.

5) Explain the concept of Scope in JavaScript (Global, Function, Block).
=>Scope basially means where a variable can be used in code.And global scope means you can use it everywhere, function scope means you can use it only inside that function.Finally block scope means you can use it only inside `{ }` like inside an if or loop.
